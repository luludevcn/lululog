---
const { url, title, description, pubDate } = Astro.props;
---

<li class="py-3 border-l border-gray-400">
    <div class="w-2 h-2 border-gray-400 rounded-md border-x-4 border-y-2 relative top-4 -left-1"></div>
    <div class="px-3">
        <span class="mr-4">{pubDate?.toString().slice(0,10)}</span>
        <a href={url}>{title}</a>
        <p class=" text-sm mt-1">{description}...</p>
    </div>
</li>
